#!/bin/bash

# Example security checks (stub)
echo "ssh_key_only|fail|PasswordAuthentication is still enabled"
echo "wan_exposure|warn|WebUI reachable on port 443 from WAN"
echo "ssl|pass|SSL cert is installed and active"
